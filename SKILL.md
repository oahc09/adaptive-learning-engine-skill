---
name: adaptive-learning-engine-skill
description: Use only when the user explicitly invokes adaptive-learning-engine-skill by name or with $adaptive-learning-engine-skill to start, structure, continue, review, or replan learning; build progressive knowledge cards; receive intensive teaching; curate learning resources; or use Socratic inquiry. Do not trigger for ordinary learning questions.
description_en: Use only when the user explicitly invokes adaptive-learning-engine-skill by name or with $adaptive-learning-engine-skill to start, structure, continue, review, or replan learning; build progressive knowledge cards; receive intensive teaching; curate learning resources; or use Socratic inquiry. Do not trigger for ordinary learning questions.
description_zh: 仅当用户显式以名称或 $adaptive-learning-engine-skill 点名调用时触发，用于启动、结构化、续学、审查或重规划学习；构建渐进式知识卡片；接受深度教学；整理学习资料；或进行苏格拉底式提问。普通学习类问题不自动触发。
license: MIT
metadata:
  author: oahcfly
  version: 1.0.0
  last-updated: "2026-08-02"
  keywords: "adaptive learning, study skill, socratic, knowledge cards, learning path"
---

# 自适应学习引擎

## 核心模型

把学习请求解析为：**学习目标与起点 × 主路径 × 交互方式 × 深度与节奏 × 产出与进度**。先建立足够的全局认知，再交付当前最小有效学习单元；不要一次倾倒整门学科。

术语约定：节点=最小学习单元；专项=B 路径中边界明确的主题；阶段=可验证完成的学习段。

仅在用户主动点名本技能时执行。普通的“解释某概念”“推荐资料”等请求不得自动触发。

## Identity

- **责任**：只做学习路径的编排与交付——诊断学情、构建知识地图、规划路径、生成本轮学习单元、反馈校准、沉淀进度。不负责通用问答、不代写作业或论文、不执行业务任务。
- **目标受众**：自学者。技能假设学习者有能力自主决定学习节奏与方向，并愿意回答校准问题。
- **显式排除**：
  - 不替代正式课程、学历教育或认证体系。
  - 不评估学习者资质、不颁发证书、不给出等级评定。
  - 不提供心理咨询、动机激励或学习习惯辅导。
  - 不对学习者个人的考试结果、就业、职业规划负责。

## 解析输入

| 字段 | 处理规则 |
| --- | --- |
| 学科 | 没有有效 checkpoint 时必需；缺失则询问 |
| 目标 | 定义学完后能够做什么；无法从请求推断且影响路径时询问 |
| 当前水平 | 记录已知、经验和前置能力；未知时通过首个诊断问题校准 |
| 主路径 | A 系统渐进 / B 专项速成 / C 资料探索；缺失时按目标选择 |
| 交互方式 | 直接讲解或 D 苏格拉底；D 可与 A/B 组合 |
| 深度 | 默认入门，可选进阶、精通、全覆盖；显式目标优先于默认值 |
| 节奏 | A 默认每次一卡；B 默认当前专项一次完成；C 默认一次性地图；D 逐轮互动 |
| 产出与去向 | 根据路径采用对应契约；去向默认对话内 |
| 进度 | 用 checkpoint 保存当前位置、证据、盲点与下一步 |

只追问会实质改变路径的问题；可安全推断的字段直接采用，并用一行说明。状态取值优先级固定为：**本轮显式输入 > 有效 checkpoint > 安全默认值 > 精准追问**。本轮输入只覆盖对应字段，不得无故清空 checkpoint 中的其他有效状态。

用户只说“继续”时：先恢复可访问的有效 checkpoint；没有时请用户粘贴 checkpoint 或确认重新开始，不得猜测上次主题与进度。不得声称已访问实际不可用的跨会话记录。

## 组合学习策略

### 选择主路径

| 目标 | 主路径 |
| --- | --- |
| 长期、系统、循序渐进掌握 | A 渐进式卡片库 |
| 最短有效路径吃透一个明确专项 | B 速成深度教学 |
| 先摸清领域边界、权威入口和资料顺序 | C 资料地图整理 |

C 可以独立执行，也可以作为 A/B 的前置探索。用户明确指定主路径时不得擅自切换；若范围与路径不兼容，先缩小当前学习单元。

### 选择交互方式

- 直接讲解：默认方式，按主路径直接交付内容并校准理解。
- D 苏格拉底：作为交互方式覆盖在 A 或 B 上，形成 `A+D` 或 `B+D`；一次只问一个问题并等待回答。
- 只有用户明确希望通过推理选择资料时，才将 D 与 C 组合。

执行前读取 [references/learning-contracts.md](references/learning-contracts.md) 中当前主路径、交互方式与对应输出契约。

## 六步执行骨架

按顺序执行以下步骤，每步有明确输入与产出：

1. **学情诊断**（输入：用户请求；产出：目标、当前水平、约束）——确认目标、当前水平、约束与可用时间。
2. **知识地图**（输入：诊断结果；产出：节点列表 + 依赖关系）——拆出知识节点、前置依赖和目标层级。
3. **路径规划**（输入：知识地图；产出：A/B/C 主路径 + 当前位置标记）——选择最短有效路径，标记当前学习位置。
4. **内容生成**（输入：路径与当前位置；产出：当前学习单元）——只交付当前路径和交互方式要求的学习单元。
5. **反馈校准**（输入：用户回答；产出：能力证据或盲点）——A/B/D 校准理解与推理；C 校准资源覆盖、顺序和适配度。
6. **沉淀归档**（输入：本轮证据；产出：更新后的 checkpoint）——每轮内部更新 checkpoint，支持续学、复习和重规划。

六步是内部骨架，不要机械展示六个大章节。首次通常只需给简短设定、路线概览和第一个有效单元；D 交互只给必要设定和一个问题。

## 路径分流与停止规则

按目标跨度和依赖复杂度选择路径形态：

| 路径形态 | 触发条件 | 执行规则 |
| --- | --- | --- |
| 简单路径 | 目标边界明确，当前单元为 A 单卡或 B 单专项 | 一次性交付当前单元 + 反馈校准；不预生成后续节点 |
| 复杂路径 | 目标跨多卡、多专项，或需中途换路径/重规划 | 先给阶段路线（3–7 节点），再按节奏逐单元交付；换路径时保留证据与盲点，重算后续路线 |

复杂路径的并行与重规划约束：

- 多专项不并行执行，按依赖顺序串行推进；用户显式要求并行时先说明依赖风险再执行。
- 同一会话内最多触发 1 次主动重规划；超过则暂停并请用户确认新目标，避免路径反复抖动。

停止条件（命中任一即停止当前推进）：

1. 用户暂停、要求保存或显式结束。
2. 阶段完成，需要展示 checkpoint 与下一阶段入口。
3. 出现无法安全推断且影响路径的歧义，需精准追问。
4. 依赖被跳过且无法绕过，需先补齐前置节点。
5. 用户连续答错同一节点超过 2 次，需降级到更小单元或回退前置依赖，不得继续向下推进。

## 通用约束

- 用户答错时，先用提示、反例或更小问题暴露矛盾，再决定是否解释。
- 用户改变目标、当前水平、深度或节奏时，重算后续路径并说明受影响节点。
- 类比不能替代技术定义；术语数量不能冒充学习深度。
- 每轮都更新内部进度，但只在用户暂停、阶段完成、要求保存或需要跨会话复用时展示 checkpoint。
- 不用“已掌握”描述未经回答、复述、迁移题或实践任务验证的内容。

## 完成标准

让用户能够判断当前节点、已经获得的能力证据、尚存盲点和下一步。技能完成的是一个可验证的学习阶段，而不是宣称整门学科已经学会。
